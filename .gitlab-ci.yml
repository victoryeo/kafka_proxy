image: openjdk:11     #Defining the Docker Image
  
stages:
  - run_test
  - post_test

run:unit_test:
  stage: run_test
  script:
    - java --version
    - ./mvnw test

post:unit_test:
  stage: post_test
  script:
    - echo $PWD
